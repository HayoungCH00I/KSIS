# 📝 한국시니어교류협회(KSIS) 포스팅 & 회원사 등록 가이드 및 AI 주문서 템플릿

이 가이드는 협회 사이트의 **비즈니스 포럼, 소모임 프로젝트, AI·마케팅·창업 교육, 그리고 회원사 스토어(쇼핑몰)** 메뉴에 신규 글(포스트)이나 회원사를 안전하고 일관성 있게 추가하기 위해 제작되었습니다.

운영진 및 작성자분들은 기존의 복잡한 코드를 직접 수정하실 필요가 없습니다. 아래의 **메뉴별 [AI 주문서]**를 복사한 뒤, `[ ]` 안의 내용을 작성하여 이미지 파일과 함께 AI 채팅창에 올려주시면 자동으로 안전하게 반영됩니다.

---

## 📂 1. 메뉴별 코드 위치 및 이미지 저장 경로

각 메뉴에 따라 세부 데이터가 기록되는 파일과 이미지가 저장되는 폴더 경로가 다릅니다. (AI가 참고하여 정확하게 작업합니다.)

| 메뉴 구분 | 세부 데이터 파일 경로 | 세부 이미지 저장 경로 | 해당 메뉴 인덱스 등록 파일 |
| :--- | :--- | :--- | :--- |
| **비즈니스 포럼** | `/src/pages/Project/forum/post[번호].ts` | `/src/images/project/forum/[포스트별폴더]/` | `/src/pages/Project/forum/index.ts` |
| **소모임: 식문화** | `/src/pages/Project/community/foodculture/posts/post[번호].ts` | `/src/images/project/community/foodculture/[포스트별폴더]/` | `/src/pages/Project/community/foodculture/index.ts` |
| **소모임: 북클럽** | `/src/pages/Project/community/bookclub/posts/post[번호].ts` | `/src/images/project/community/bookclub/[포스트별폴더]/` | `/src/pages/Project/community/bookclub/index.ts` |
| **소모임: 로컬탐방** | `/src/pages/Project/community/local/posts/post[번호].ts` | `/src/images/project/community/local/[포스트별폴더]/` | `/src/pages/Project/community/local/index.ts` |
| **AI·마케팅·창업 교육** | `/src/pages/Project/education/post[번호].ts` | `/src/images/project/AI/[포스트별폴더]/` | `/src/pages/Project/education/index.ts` |
| **회원사 스토어(쇼핑몰)** | `/src/pages/Project/Shopping.tsx` | `/src/images/project/shopping-mall/` | 파일 내 `companies` 배열에 직접 추가 |
| **통합 일정 캘린더** | `/src/initialData.ts` | (해당 없음) | `INITIAL_EVENTS` 배열 내에 직접 추가 |
| **게시판(공지사항/인사이트)** | `/src/pages/Board.tsx` | (해당 없음) | `NOTICES_DATA` 또는 `INSIGHTS_DATA` 배열 내에 직접 추가 |

---

## 🖼️ 2. 이미지 업로드 요령
- **AI 대화창에 올리기**: 본문을 입력하시기 전이나 함께 **대화창 하단의 파일 첨부(+) 아이콘을 통해 이미지를 직접 첨부**하거나 **드래그 앤 드롭**으로 올려주세요.
- **이미지 매칭**: 아래 주문서의 본문 작성 칸에 `(Image 1)`, `(Image 2)` 형태로 표기해 주시면 AI가 올려주신 이미지 파일을 알맞게 가공하여 등록합니다.

---

## 🎫 3. [주문서 1] 일반 포스팅용 (포럼 / 소모임 / AI·마케팅 교육)

> **💡 사용 방법:** 아래 점선 안의 텍스트 전체를 복사하여 `[ ]` 부분을 적절히 수정하신 뒤, 관련 이미지들과 함께 AI 대화창에 그대로 전송해 주세요.

```markdown
---
[신규 포스팅 작성 요청 주문서]

안녕하세요! 한국시니어교류협회 공식 포스팅 작성 주문서입니다.
아래 작성 가이드 및 데이터를 기반으로 기존 UI/레이아웃을 절대 수정하지 않고(UI freeze), 오직 신규 데이터 파일 추가 및 인덱스 등록만 수행해 주세요.

■ 필수 지침 (AI 수행 지침):
1. 기존 UI 파일(Project.tsx, Forum.tsx, Education.tsx 등)의 레이아웃, 스타일, 여백 등 시각적 디자인은 절대 수정하지 마세요.
2. 업로드/첨부된 이미지들은 해당하는 메뉴의 전용 정적 이미지 폴더 아래에 새 프로젝트 전용 하위 폴더를 만들어서 저장해 주세요:
   - 비즈니스 포럼: /src/images/project/forum/[새폴더명]/
   - 소모임 프로젝트(식문화): /src/images/project/community/foodculture/[새폴더명]/
   - 소모임 프로젝트(북클럽): /src/images/project/community/bookclub/[새폴더명]/
   - 소모임 프로젝트(로컬탐방): /src/images/project/community/local/[새폴더명]/
   - AI·마케팅·창업 교육: /src/images/project/AI/[새폴더명]/
3. 신규 게시물은 고유한 ID(기존 ID 중 가장 큰 값 + 1)를 가지며, 위 매핑 가이드에 기재된 정확한 경로에 새로운 post[번호].ts 파일로 생성해야 합니다.
4. 새로 만든 post[번호].ts 파일을 각 카테고리의 index.ts에 올바르게 import하고 내보내는 배열에 추가(등록)해 주세요.

============================================================
1. 메뉴 구분 (택 1): [ 예: 비즈니스 포럼 / 소모임 프로젝트(식문화) / 소모임 프로젝트(북클럽) / 소모임 프로젝트(로컬탐방) / AI·마케팅·창업 교육 ]
   - 입력: [선택한 메뉴를 적어주세요]

2. 제목 (name):
   - 입력: [여기에 포스팅 제목을 입력하세요]

3. 카테고리 상세 명칭 (categoryName):
   - 입력: [예: 비즈니스 포럼, 식문화, 북클럽, 로컬탐방, 디지털 역량 강화 등]

4. 강연자 / 모임 리더 (specialty):
   - 입력: [예: 원동업 성수동편집장 / 길기태 강사 등]

5. [교육 메뉴 전용] 교육 기간 (duration):
   - 입력: [예: 4주 과정 (매주 토요일) / 해당하지 않으면 비워두기]

6. 한 줄 요약 소개 (desc):
   - 입력: [여기에 메인 카드에 노출될 매력적인 한 줄 요약 소개를 적어주세요]

7. 게시 일자 (date):
   - 입력: [YYYY.MM.DD 형식으로 입력, 예: 2026.06.30]

8. 본문 상세 내용 및 이미지 구성 (contentBlocks):
   ※ 본문은 글(Text), 소제목(Heading), 강조박스(Quote), 개별이미지(Image), 슬라이더 갤러리(Gallery)를 섞어서 자유롭게 구성할 수 있습니다. 
   ※ 첨부해주신 이미지는 위에서부터 순서대로 '이미지1', '이미지2'로 매칭됩니다.

   [본문 구성 템플릿 시작]
   - (Text): [본문 첫 단락 내용을 적어주세요]
   
   - (Image): [첫 번째 첨부 이미지 매칭 - 간단한 이미지 설명 입력]
   
   - (Heading): [중간 소제목이 필요하다면 입력]
   
   - (Quote): [📍 장소 : 황금바늘 / 📅 일시 : 2026년 6월 13일 등 일시 및 핵심 요약 입력]
   
   - (Text): [본문 두 번째 단락 내용을 적어주세요]
   
   - (Gallery): [슬라이더로 묶어서 보여줄 이미지 번호들을 쉼표로 구분하여 적어주세요. 예: 이미지2, 이미지3, 이미지4]
   
   - (Text): [마무리 단락 내용을 적어주세요]
   [본문 구성 템플릿 끝]

============================================================
첨부된 이미지가 있다면 위 지정을 바탕으로 정적 리소스에 추가하고 임포트하여 게시글을 등록해 주세요. 완료 후 빌드 및 린트 검증까지 마친 뒤 요약해 주시기 바랍니다.
---
```

---

## 🛍️ 4. [주문서 2] 회원사 스토어용 (쇼핑몰 / 기업 정보)

> **💡 사용 방법:** 아래 점선 안의 텍스트 전체를 복사하여 `[ ]` 부분을 적절히 수정하신 뒤, **대표 이미지 및 로고 이미지**와 함께 AI 대화창에 그대로 전송해 주세요.

```markdown
---
[회원사 스토어 등록 요청 주문서]

안녕하세요! 한국시니어교류협회 공식 회원사 등록 주문서입니다.
아래 작성 가이드 및 데이터를 기반으로 기존 UI/레이아웃을 절대 수정하지 않고(UI freeze), 오직 `/src/pages/Project/Shopping.tsx`의 `companies` 데이터 배열에만 안전하게 새 회원사 정보를 추가해 주세요.

■ 필수 지침 (AI 수행 지침):
1. 기존 UI 파일(Shopping.tsx 등)의 레이아웃, 스타일, 여백 등 시각적 디자인은 절대 수정하지 마세요.
2. 업로드/첨부된 대표 이미지와 로고 이미지는 다음 경로에 저장해 주세요:
   - 이미지 경로: `/src/images/project/shopping-mall/[영어회사명]_hero.webp`
   - 로고 경로: `/src/images/project/shopping-mall/[영어회사명]_logo.png`
3. 저장한 이미지들을 `Shopping.tsx` 상단에 올바르게 import해 주세요.
4. `Shopping.tsx` 파일 내 `companies` 배열에 새 회원사 정보를 객체 형태로 안전하게 추가해 주세요. ID는 기존 최대 ID + 1로 자동 부여합니다.

============================================================
1. 회사명 (name):
   - 입력: [예: 주식회사 캘리엠]

2. 대표자 / 직책 (representative):
   - 입력: [예: 박서영 대표자 / 회원님]

3. 카테고리 상세 분류 (categoryName):
   - 입력: [예: 기타소품 / 디자인]

4. 슬로건 / 한 줄 소개 (tagline):
   - 입력: [예: 일상의 작은 순간에 특별한 가치를 더하는 디자인 기업]

5. 회사 상세 소개 내용 (descParagraphs):
   ※ 본문은 여러 단락으로 줄바꿈해서 구성할 수 있습니다.
   - 단락 1: [첫 번째 소개 문장 또는 단락]
   - 단락 2: [두 번째 소개 문장 또는 단락 (필요시)]

6. 공식 홈페이지 또는 온라인 숍 링크 (link):
   - 입력: [예: https://calli-m.com]

7. 첨부 이미지 파일 매칭 정보:
   - 대표 이미지: [첨부한 기업 대표 이미지 파일명 기재]
   - 로고 이미지: [첨부한 기업 로고 이미지 파일명 기재 (없다면 비워두기)]

============================================================
첨부된 이미지를 프로젝트 정적 디렉토리에 맞추어 저장하고 `Shopping.tsx`에 연동해 주세요. 완료 후 빌드 및 린트 검증까지 마친 뒤 요약해 주시기 바랍니다.
---
```

---

## 📅 5. [주문서 3] 통합 일정 캘린더 일정 등록용

> **💡 사용 방법:** 아래 점선 안의 텍스트 전체를 복사하여 `[ ]` 부분을 적절히 수정하신 뒤, AI 대화창에 그대로 전송해 주세요. (괄호 안에 예시가 기입되어 있습니다.)

```markdown
---
[신규 일정 등록 요청 주문서]

안녕하세요! 한국시니어교류협회 공식 일정 등록 주문서입니다.
아래 작성 가이드 및 데이터를 기반으로 기존 UI/레이아웃을 절대 수정하지 않고(UI freeze), 오직 `/src/initialData.ts`의 `INITIAL_EVENTS` 데이터 배열에만 안전하게 새 일정 정보를 추가해 주세요.

■ 필수 지침 (AI 수행 지침):
1. 기존 UI 파일(CalendarPage.tsx, CalendarSection.tsx 등)의 레이아웃, 스타일, 여백 등 시각적 디자인은 절대 수정하지 마세요.
2. 새 일정은 고유한 ID(기존 ID 중 가장 큰 값 + 1)를 문자열 형태로 가집니다.
3. `/src/initialData.ts` 파일의 `INITIAL_EVENTS` 배열 마지막 요소 뒤에 새 일정 객체를 완벽하게 정의해서 추가해 주세요.

============================================================
1. 일정 날짜 (date):
   - 입력: [YYYY-MM-DD 형식으로 입력, 예: 2026-07-04 (년-월-일 형식으로 기입)]

2. 일정 제목 (title):
   - 입력: [여기에 일정 제목 입력, 예: 제34회 비즈니스교류회 - 돈 되는 스토리는 따로 있다 (포럼 명칭 혹은 대표 제목)]

3. 카테고리 구분 (category):
   - 입력: [예: forum (비즈니스 포럼이면 'forum', AI·마케팅·창업 교육이면 'education', 친목/문화/소모임 및 기타면 'other' 기입)]

4. 간단 정보 (description):
   - 입력: [여기에 일정을 설명하는 한 줄 요약 입력, 예: 내가 살아온 시간을 다시 읽고, 그 안에서 나만의 콘텐츠와 브랜드 자산을 발견하는 시간 (세부 부제목이나 취지)]

5. 시간 (time):
   - 입력: [예: 08:00 - 10:00 (또는 '15:00 - 18:00', 시작시간만 있을 시 '17:00 -' 등 자유롭게 기입)]

6. 장소 (location):
   - 입력: [예: 교류협회 사무실 (마포구 신촌로 200-1, 황금바늘빌딩 2층, 이대역 5번 출구 앞) (모임 구체적 장소 기입)]

7. 기본 대상자 / 안내사항 (rsvps):
   - 입력: [예: 한국시니어교류협회 회원, 회원 추천 동반인, 일반 참가자 (행사에 기본 참여 가능한 추천 대상자 목록 기입)]

8. 네이버 카페 링크 (link):
   - 입력: [예: https://cafe.naver.com/ksis1/641 (세부 공지 네이버 카페 주소 기입, 없을 시 비워두기)]

============================================================
작성한 정보를 바탕으로 `initialData.ts`에 일정을 등록해 주세요. 완료 후 빌드 및 린트 검증까지 마친 뒤 요약해 주시기 바랍니다.
---
```

---

## 📢 6. [주문서 4] 게시판(BOARD) 공지사항 및 인사이트 등록용

> **💡 사용 방법:** 아래 점선 안의 텍스트 전체를 복사하여 `[ ]` 부분을 적절히 수정하신 뒤, AI 대화창에 그대로 전송해 주세요. (괄호 안에 예시가 기입되어 있습니다.)

```markdown
---
[신규 공지사항 / 인사이트 등록 요청 주문서]

안녕하세요! 한국시니어교류협회 공식 게시판(BOARD) 등록 주문서입니다.
아래 작성 가이드 및 데이터를 기반으로 기존 UI/레이아웃을 절대 수정하지 않고(UI freeze), 오직 `/src/pages/Board.tsx` 파일 내의 `NOTICES_DATA` 또는 `INSIGHTS_DATA` 데이터 배열에만 안전하게 새 게시물 정보를 추가해 주세요.

■ 필수 지침 (AI 수행 지침):
1. 기존 UI 파일(Board.tsx의 본문 렌더링 영역 등)의 레이아웃, 스타일, 여백 등 시각적 디자인은 절대 수정하지 마세요.
2. 새 포스트는 고유한 ID(공지사항의 경우 'n' + (기존 최대 번호+1), 인사이트의 경우 'i' + (기존 최대 번호+1))를 문자열 형태로 가집니다. (예: 공지사항은 n2, 인사이트는 i2 등)
3. `/src/pages/Board.tsx` 파일 상단부의 `NOTICES_DATA` 또는 `INSIGHTS_DATA` 배열의 맨 앞(최신글이 위에 보이도록) 또는 배열 마지막에 새 포스트 객체를 안전하게 추가해 주세요.

============================================================
1. 게시판 구분 (category_tab):
   - 입력: [공지사항 이면 'notice', 인사이트 면 'insight' 기입]

2. 카테고리 상세 명칭 (category):
   - 입력: [예: 행사 안내 (또는 '협회 소식', 인사이트의 경우 '여행', '칼럼', '에세이' 등 기입)]

3. 게시물 제목 (title):
   - 입력: [예: 제34회 비즈니스교류회 - "돈 되는 스토리는 따로 있다" 참가 신청 안내 (또는 '시니어 디지털 역량 강화 칼럼')]

4. 게시 일자 (date):
   - 입력: [YYYY.MM.DD 형식으로 입력, 예: 2026.06.30]

5. 네이버 카페 링크 (url):
   - 입력: [예: https://cafe.naver.com/ksis1/641 (세부 본문 네이버 카페 글 주소 기입)]

============================================================
작성한 정보를 바탕으로 `Board.tsx`의 해당 데이터 배열에 신규 항목을 연동해 주세요. 완료 후 빌드 및 린트 검증까지 마친 뒤 요약해 주시기 바랍니다.
---
```

---

## 📸 7. [주문서 5] 활동 갤러리(GALLERY) 등록용 (게시글 자동 연동)

> **💡 사용 방법:** 갤러리는 단순히 사진만 따로 올리는 공간이 아닌, **이미 등록된 게시글(포럼, 소모임, 교육 등)의 내용을 연동**하는 서비스입니다. 
> 이미 작성된 글의 **정확한 제목**만 적어주시면, AI가 사이트 전체 소스코드에서 해당 게시글을 자동으로 찾은 뒤 **그 안에 삽입된 모든 이미지들을 자동으로 추출하여 갤러리에 추가**합니다. 아래 점선 안의 텍스트 전체를 복사하고 `[ ]` 부분을 작성해 주세요.

```markdown
---
[신규 활동 갤러리 자동 등록 요청 주문서]

안녕하세요! 한국시니어교류협회 공식 활동 갤러리(GALLERY) 등록 주문서입니다.
기존 UI/레이아웃을 절대 수정하지 않고(UI freeze), 오직 `/src/pages/Board.tsx` 내의 `GALLERY_ITEMS` 배열에만 안전하게 추가해 주세요.

■ 필수 지침 (AI 수행 지침):
1. 기존 UI 파일(Board.tsx의 갤러리 렌더링 영역 등)의 레이아웃, 스타일, 여백 등 시각적 디자인은 절대 수정하지 마세요.
2. 새 갤러리 항목은 고유한 ID(기존 ID 중 가장 큰 값 + 1, 숫자 형식)를 가집니다.
3. 제공받은 [연동 게시글 제목]으로 전체 프로젝트 디렉토리 내의 포스팅 파일(postX.ts 등)을 검색하여 일치하는 글을 찾으세요.
4. 해당 글 파일에 정의된 `contentBlocks`를 분석하여 `image` 및 `gallery` 블록에 들어간 모든 이미지 변수/임포트를 추출해 주세요.
5. 추출한 이미지들 중 첫 번째 이미지를 대표 썸네일(`img`)로 지정하고, 전체 이미지 리스트를 `images` 배열에 바인딩하여 `Board.tsx` 파일 내 `GALLERY_ITEMS` 배열에 추가하고 필요시 파일 상단에 해당 이미지를 import해 주세요.

============================================================
1. 이미 사이트에 등록 완료된 게시글 제목 (title - 정확하게 입력):
   - 입력: [예: 제34회 비즈니스교류회 - 돈 되는 스토리는 따로 있다 (또는 '새로운 텃밭 가꾸기와 건강 요리 교실')]

============================================================
지정해주신 게시글 제목의 포스팅 데이터를 찾아 그 안의 이미지를 모두 자동 연동하여 활동 갤러리에 등록해 주세요. 완료 후 빌드 및 린트 검증까지 마친 뒤 요약해 주시기 바랍니다.
---
```

---

## 💡 본문 블록 타입 상세 설명 (작성 보조 가이드)

일반 포스팅 등록 시 본문 상세 내용에 혼합하여 사용할 수 있는 개별 블록들의 실제 형태입니다.

* **Text**: 일반 글 문단입니다. 이야기를 풀어나갈 때 자유롭게 사용합니다.
* **Heading**: 굵고 강조된 중간 소제목입니다. 긴 글을 끊어 읽기 좋게 만듭니다.
* **Quote**: 장소, 시간, 회비 등 핵심적인 고정 정보들을 카드 형태의 은은한 배경 상자 안에 담아 시각적으로 강조해 줍니다.
* **Image**: 중요한 사진 한 장을 크고 웅장하게 단독 배치할 때 사용합니다.
* **Gallery**: 현장 분위기를 보여주는 여러 장의 사진들을 묶어 좌우 화살표를 통해 슬라이드로 쾌적하게 볼 수 있도록 만듭니다.
